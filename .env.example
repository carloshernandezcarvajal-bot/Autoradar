# =====================
# Railway (Backend)
# =====================
# DATABASE_URL lo provee Railway automaticamente al agregar PostgreSQL plugin.
# Railway da: postgresql://<user>:<pass>@<host>:<port>/<db>
# La app convierte automaticamente a postgresql+asyncpg:// (no hace falta tocar)

SECRET_KEY=38360764c022310fa991220eea4cdc9094c12cb1972f6005232d5168ebbd8309
ENCRYPTION_KEY=c4d1d7c02b868d3e6266cbe6637de7f14b95c55e95240b78169265bf2160de70
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
CORS_ORIGINS=http://localhost:3000,https://<frontend-vercel>.vercel.app
ENVIRONMENT=production

# Generar nuevas keys (si queres):
#   python -c "import secrets; print(secrets.token_hex(32))"

# =====================
# Vercel (Frontend)
# =====================
# Configurar en Vercel > Project Settings > Environment Variables:
#   NEXT_PUBLIC_API_URL = https://<backend-railway>.railway.app
